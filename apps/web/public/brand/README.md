# REPLAN public brand assets

These files are deployment-ready copies of the approved assets in `assets/brand/replan/png/`.

Use root-relative URLs such as `/brand/replan-wordmark.png` and `/brand/replan-mark-64.png` in the Next.js app. Edit or replace the canonical files under `assets/brand/replan/` first, then refresh these copies.
